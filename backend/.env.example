# -----------------------------------------------------------------------------
# Kraken TypeScript Coverage Improver - Environment Configuration
# -----------------------------------------------------------------------------

# Application Environment (development, test, production)
NODE_ENV=development

# Port for the backend API to listen on
PORT=3000

# SQLite Database Path (Defaults to database.sqlite if not provided)
DB_PATH=database.sqlite

# GitHub Personal Access Token (Required)
# Needs 'repo' scope for private repositories, or at minimum 'public_repo' for public ones.
# Used for cloning, creating branches, and opening Pull Requests.
GITHUB_TOKEN=your_github_personal_access_token_here

# Redis Configuration (Required for BullMQ Job Queues)
# If using docker-compose, this is typically handled automatically.
REDIS_HOST=redis
REDIS_PORT=6379

# Google Gemini AI Model Selection
# Default is gemini-2.0-flash-lite. Can be overridden for better reasoning models.
GEMINI_MODEL=gemini-2.0-flash-lite
